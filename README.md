# TicketSystem

Run the support portal with:

```bash
npm start
```

The app is available at `http://localhost:3000`.

If port 3000 is already in use, start it on another port:

```bash
PORT=3001 npm start
```
